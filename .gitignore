__pycache__/
*.pyc
*.swp
Pasted image.png
.env
.defn/
uv.lock

# Source images are copyright — only braille art ships in the repo.
# Run `meme --bake-all` to regenerate from img_url.
frames/*.jpg
frames/*.jpeg
frames/*.png
frames/*.gif
